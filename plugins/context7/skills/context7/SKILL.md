---
name: context7
description: Use Context7 MCP when current library, framework, SDK, or API documentation is needed.
---

Use Context7 for library/API documentation, setup steps, migration guidance, and code examples that may depend on current versions.

When the user names a known Context7 library ID, prefer the slash syntax directly, for example `/supabase/supabase`.

If the user does not know the library ID, resolve the library first, then fetch focused docs for the specific question. Keep retrieved docs minimal and summarize instead of dumping large excerpts.